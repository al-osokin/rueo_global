# Руководство по ручному редактированию переводов

## Обзор

Добавлена возможность вручную указывать правильный разбор переводов в интерфейсе `/admin/review`, когда автоматический парсер ошибается.

## Как использовать

### В интерфейсе AdminReview

1. Откройте статью в `/admin/review`
2. Для групп с флагом **"требуется проверка"** (красный фон) отображается список гипотез с радиокнопками
3. Среди гипотез всегда есть вариант **"Свой вариант"**
4. Выберите радиокнопку **"Свой вариант"** — появится текстовое поле
5. Введите правильные варианты перевода, разделяя синонимы вертикальной чертой `|`
   - Например: `выступать в роли адвоката | выступать в роли защитника`
6. Нажмите **"Дальше"** для сохранения

### Принятие автоматического варианта

Если автоматический разбор правильный (например, "адвокатствовать | работать адвокатом"):
1. Просто выберите соответствующий вариант радиокнопкой
2. Оставьте "Свой вариант" невыбранным
3. Нажмите **"Дальше"** для сохранения

### Пример 1: Простой случай (статья [advokat|o], art_id=267)

**Проблема:** Группа 2 (`[~i]`) неправильно разбирается как:
```
выступ`ать в р`оли адвок`ата | защ`итника
```

**Доступные гипотезы:**
- 🔘 **Как в источнике**: `выступ`ать в р`оли адвок`ата | защ`итника`
- 🔘 **Свой вариант**: _(поле для ввода)_

**Решение:** Выбираем "Свой вариант" и вводим:
```
выступать в роли адвоката | выступать в роли защитника
```

### Пример 2: С расширенными комбинациями (статья [abon|i], art_id=66)

**Ситуация:** Парсер предлагает расширенный вариант для `[~ebl/a]`

**Доступные гипотезы:**
- 🔘 **Расширенные комбинации**: `дост`упный для абон`ирования | им`еющийся в подп`иске | дост`упный для подп`иски`
- 🔘 **Как в источнике**: `дост`упный для абон`ирования | для подп`иски | им`еющийся в подп`иске`
- 🔘 **Свой вариант**: _(поле для ввода)_

**Решение:** 
- Если расширенный вариант правильный → выбираем "Расширенные комбинации"
- Если исходный вариант лучше → выбираем "Как в источнике"
- Если нужен другой вариант → выбираем "Свой вариант" и вводим текст

### Особенности

- Кандидат "Свой вариант" добавляется **только** для групп с флагом `requires_review`
- Количество кандидатов зависит от ситуации:
  - **2 кандидата**: "Как в источнике" + "Свой вариант" (когда нет расширений)
  - **3 кандидата**: "Расширенные комбинации" + "Как в источнике" + "Свой вариант" (когда парсер предложил расширение)
- Если уже есть сохранённый ручной вариант, он отображается в списке items кандидата "Свой вариант"
- Знаки ударения (`\``) не обязательны — полнотекстовый поиск их игнорирует
- Можно указать любое количество синонимов, разделяя их `|`
- Статья перестаёт требовать проверки для группы, если:
  - Выбран любой автоматический кандидат ("Расширенные комбинации" или "Как в источнике")
  - ИЛИ выбран "Свой вариант" и заполнено текстовое поле

## Интеграция с полнотекстовым поиском

### Текущая реализация

Ручные варианты уже интегрированы в процесс подготовки данных:

1. **Хранение:** `ArticleParseState.resolved_translations.groups[group_id].manual_override`
2. **Извлечение:** `ArticleParserService._build_result()` собирает все `manual_override` из групп
3. **Индексация:** Фразы из `manual_override` попадают в `translations` через `collect_translation_phrases()`

### Для генерации полнотекстового индекса

Когда будете реализовывать шаг 2.1 (подготовка данных для индексации), используйте:

```python
from app.services.article_parser import ArticleParserService

with SessionLocal() as session:
    parser = ArticleParserService(session)
    result = parser.parse_article_by_id('eo', art_id, include_raw=True)
    
    # result.translations содержит:
    # - Автоматически распознанные переводы
    # - Ручные варианты из manual_override
    full_text_phrases = result.translations
```

### Обработка для индекса

```python
def normalize_for_index(phrases: List[str]) -> str:
    """Нормализация фраз для полнотекстового поиска"""
    normalized = []
    for phrase in phrases:
        # Убираем ударения
        clean = phrase.replace('`', '').replace("'", '')
        # Дополнительная нормализация (замена ё/е, etc.)
        clean = clean.replace('ё', 'е')
        normalized.append(clean)
    return ' '.join(normalized)
```

## Структура данных

### База данных

```json
{
  "groups": {
    "group_0": {
      "accepted": true,
      "selected_candidate": "expanded",
      "manual_override": "выступать в роли адвоката | выступать в роли защитника"
    }
  }
}
```

### API Response (GET /admin/articles/{lang}/{art_id})

```json
{
  "groups": [
    {
      "group_id": "group_2",
      "requires_review": true,
      "items": ["выступ`ать в р`оли адвок`ата", "защ`итника"],
      "selected_candidate": "manual",
      "manual_override": "выступать в роли адвоката | выступать в роли защитника",
      "candidates": [
        {
          "id": "base",
          "title": "Как в источнике",
          "items": ["выступ`ать в р`оли адвок`ата", "защ`итника"]
        },
        {
          "id": "manual",
          "title": "Свой вариант",
          "items": ["выступать в роли адвоката", "выступать в роли защитника"]
        }
      ]
    }
  ]
}
```

## Логика проверки статуса

Статья считается **проверенной** (не требует ревью), если для каждой группы выполняется одно из условий:

1. `accepted: true` — группа явно принята
2. `selected_candidate: "manual"` И `manual_override` указан и не пустой
3. `selected_candidate` не равен "manual" (выбран любой автоматический вариант)
4. `requires_review: false` И группа не автогенерирована

## Тестирование

```bash
# Загрузить статью 267
PYTHONPATH=backend python - <<'PY'
from app.database import SessionLocal, init_db
from app.services.article_review import ArticleReviewService

init_db()
with SessionLocal() as session:
    service = ArticleReviewService(session)
    result = service.load_article('eo', 267)
    
    # Найти группу 2
    group_2 = result['groups'][2]
    print(f"Requires review: {group_2['requires_review']}")
    print(f"Items: {group_2['items']}")
    print(f"Manual override: {group_2.get('manual_override', '')}")
PY
```

## Дальнейшие улучшения

1. **Валидация:** Можно добавить проверку формата `manual_override` на фронтенде
2. **История:** Отслеживание изменений `manual_override` через `article_parse_notes`
3. **Статистика:** Подсчёт статей с ручными исправлениями
4. **Экспорт:** Возможность выгрузить все ручные варианты для анализа парсера
